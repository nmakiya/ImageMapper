<%= form_for :image, url: '/images/new', html: { multipart: true } do |form| %>
  <%= form.file_field :image %>
  <%= form.submit %>
<% end %>

<% @user.images.each do |i| %>
  <%= i.image.url %>
  <%= link_to "#{i.id}", "/images/#{i.id}" %>
<% end %>
